info:
  name: JDS - Caller - Bad Experience - Get Progressive Template Values
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/v1/api/progressive-profile-view/workspace-id/{{workspaceId}}/person-id/{{personId}}/template-id/{{profileTemplateId}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
