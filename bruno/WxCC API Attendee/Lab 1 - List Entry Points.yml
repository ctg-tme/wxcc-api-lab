info:
  name: List Entry Points
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/organization/{{org_id}}/v2/entry-point"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
