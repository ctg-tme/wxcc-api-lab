info:
  name: WxCC - Event List Types
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/v2/event-types"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: ""
      value: "{{org_id}}"

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
